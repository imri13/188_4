extends template 
block content 
    nav.navbar
        .navbar-links
            a(href='/') Log out
            a(href='/myPlaces') Favorites
    .container
        h1 חיפוש בילוי
        form#main-form(action='/mainForm', method='post')
            .form-group
                label(for='area') אזור בארץ
                select#area(name='area')
                    // Options from the database will be added dynamically here
                    option מרכז
                    option חיפה
                    option ירושלים
            .form-group
                label(for='partners') שותפים לבילוי
                select#partners(name='partners')
                    // Options from the database will be added dynamically here
                    option חברים
                    option משפחה
                    option חבר/ה
            button(type='submit') מצא לי בילוי